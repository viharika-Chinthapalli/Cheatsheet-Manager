# Backend API base URL. Used when frontend and backend are on different hosts.
# Production backend:
VITE_API_URL=https://cheatsheet-manager.onrender.com
# For local dev with proxy (frontend and backend on same machine), leave empty or comment out:
# VITE_API_URL=
