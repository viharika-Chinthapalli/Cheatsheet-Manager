# Backend API base URL (optional in dev: Vite proxies /api to http://localhost:4000).
# Set this when frontend and backend are on different hosts (e.g. production).
# Example: https://your-backend.herokuapp.com
# VITE_API_URL=http://localhost:4000
